# Casos de Prueba para el Parser

# 1. Operaciones aritméticas básicas 
actual(no modificar): 5 + 3 * 2      

# 2. Precedencia con paréntesis
actual(no modificar): (5 + 3) * 2

# 3. Operaciones lógicas
actual(no modificar): true and false or true

# 4. Operaciones de comparación
actual(no modificar): 10 > 5 and 3 < 7      

# 5. Binding de variables simple
actual: let(x)(10)(x + 5)
modificar para que funcione asi: let x = 10 in x + 5

# 6. Binding de variables anidado
actual: let(x)(10)(let(y)(20)(x + y))
modificar para que funcione asi: let x = 10 in let y = 20 in x + y

# 7. Expresión condicional simple
if(5 > 3)(42) (no va , solo con if else)
- no va

# 8. Expresión condicional con else
modificar para que funcione asi( no se como esta  trabajndo el if en lo actual): if(false) 10 
 else 20 

# 9. Conversión de tipos
actual(no modificar): itos(42)
actual(no modificar) : rtos(3.14)

# 10. Concatenación de strings
actual(no modificar): "Hola" # " mundo"

# 11. Concatenación con conversión
actual(no modificar): itos(42) # " es la respuesta"

# 12. Print simple
actual(no modificar): print("Hola mundo")

# 13. Print con variable
actual: let(mensaje)("Buenos días")(print(mensaje))
modificar para que funcione asi: let mensaje = "buenos dias" in print(mensaje)

# 14. Operación compleja con variables y condicionales
modificar para que funcione asi: let(x)(10)(let(y)(5)(if(x > y)(x + y) else (x - y)))
cambiar en el mismo
# 15. Pares
actual: pair(1)(2)
cambiar a:  (1 , 2) -> agregar token_coma

# 16. Números reales
actual(no modificar): 3.14 + 2.86

# 17. Operación con módulo
actual(no modificar): 17 % 5

# 18. Negación lógica
actual(no modificar): not false

# 19. XOR lógico
actual(no modificar): true xor false

# 20. Comparaciones múltiples
actual(no modificar): 5 <= 10 and 3 >= 1

#21. Funciones simples
actual: let(cuadrado)(fun(cuadrado)(x)(x * x ))
(
   print(cuadrado(5))
)
modificar para que funcione asi: fun cuadrado(x) x*x

#22. Funciones Recursivas
actual: let(cuadrado)(fun(cuadrado)(x)(x + 5 ))
(
   print(cuadrado(cuadrado(5)))
)
modificar para que funcione asi: fun pow(x, y) if(y == 0) 1
              else x * pow(x, y-1)

#21. Funciones multiples
actual: let(def_cuadrado)(fun(cuadrado)(x)(x * x ))
(
    let(def_doble)(fun(doble)(x)(x + x ))
    (
        print(cuadrado(5) + doble(10))
    ) 
)
cambiar para que funciones con la semantica de las funciones anteriores